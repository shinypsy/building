import sys

research_addition = """
## 7. 양천구/구로구 건축물 사용승인현황 데이터 구조 분석 및 병합 리서치 (2026-05-21)
- **리서치 목표**: 양천구 파일의 '중대형건물_관리현황' 시트 형식을 참조하여, 구로구 데이터 중 연면적 10,000 이상 30,000 미만인 건물을 추출해 새로운 시트를 생성하기 위한 구조 파악.
- **양천구 파일 ('중대형건물_관리현황' 시트) 컬럼**:
  - `연번`, `건물명`, `대지위치`, `연면적(제곱미터)`, `주용도`, `건물관리업체 여부`, `관리업체명`, `관리업체 연락처`, `허가일`, `사용승인일`, `최대지상층수`, `설계사무소명` (총 12개)
- **구로구 파일 ('사용승인_통합' 시트) 컬럼**:
  - `건축구분`, `건물명`, `대지위치`, `지목`, `대지면적(㎡)`, `건축면적(㎡)`, `연면적(㎡)`, `건폐율(%)`, `용적률(%)`, `구조`, `허가일`, `착공처리일`, `사용승인일`, `최대지상층수`, `최대지하층수`, ... , `주용도`, `설계사무소명` 등 (총 54개 컬럼)
- **결론 및 추출 전략**:
  - 구로구 데이터(`사용승인_통합`)에서 `연면적(㎡)` 컬럼의 값이 >= 10000 이고 < 30000 인 행을 필터링.
  - 양천구의 12개 컬럼 양식에 맞추어 구로구 데이터를 매핑(Mapping)함.
    - `건물명` -> `건물명`
    - `대지위치` -> `대지위치`
    - `연면적(제곱미터)` -> `연면적(㎡)`
    - `주용도` -> `주용도`
    - `허가일` -> `허가일`
    - `사용승인일` -> `사용승인일`
    - `최대지상층수` -> `최대지상층수`
    - `설계사무소명` -> `설계사무소명`
    - (나머지 `연번`, `건물관리업체 여부`, `관리업체명`, `관리업체 연락처`는 빈 값 처리 후 나중에 일련번호 부여 등 수행)
  - 파이썬 `pandas`를 활용하여 필터링 및 매핑을 100% 자동화하여 새로운 엑셀 파일(혹은 시트) 생성 가능함을 검증완료.
"""

with open(r'd:\Dev\Project\sample\doc\research.md', 'a', encoding='utf-8') as f:
    f.write(research_addition)

plan_addition = """
# 구로구 중대형 건물(연면적 1만~3만㎡) 관리현황 데이터베이스 및 신규 시트 구축 구현 계획 (plan.md) (2026-05-21)

## 1. 접근 방식 및 코드 구조 상세 설명
- **대상 파일**: `구로구 사용승인 현황(2020년이후).xlsx`
- **목표**: 
  1. 원본 엑셀 `사용승인_통합` 시트에서 `10,000 <= 연면적(㎡) < 30,000` 조건에 해당하는 중대형 건물을 선별.
  2. 양천구 파일의 '중대형건물_관리현황' 시트 형식(12개 컬럼)에 맞게 데이터 추출 및 변환.
  3. 변환된 데이터를 바탕으로 구로구 엑셀 파일 내에 **`중대형건물_관리현황`** 이라는 신규 시트를 생성하여 저장.
- **상세 처리 로직**:
  - `pandas`를 사용해 파일을 읽고 필터링 수행.
  - 컬럼 이름 불일치 문제(`연면적(㎡)` 등)를 해결하기 위해 컬럼 매핑(rename) 적용.
  - 비어있는 필수 관리 컬럼(`건물관리업체 여부`, `관리업체명`, `관리업체 연락처`, `연번`)을 `NaN` 또는 빈 문자열로 초기화 추가.
  - 기존 구로구 파일에 덮어쓰거나(openpyxl 엔진 if_sheet_exists='replace') 시트를 추가함.

## 2. 파일 경로 (File Paths)
- 대상 및 출력 파일: `d:\Dev\Project\sample\구로구 사용승인 현황(2020년이후).xlsx` [MODIFY]
- 가공 스크립트: `d:\Dev\Project\sample\create_guro_sheet.py` [NEW]

## 3. 코드 스니펫 (Code Snippet)
```python
import pandas as pd
import sys

sys.stdout.reconfigure(encoding='utf-8')

guro_file = '구로구 사용승인 현황(2020년이후).xlsx'
# 1. 파일 읽기
df = pd.read_excel(guro_file, sheet_name='사용승인_통합')

# 2. 1만 ~ 3만㎡ 필터링
filtered = df[(df['연면적(㎡)'] >= 10000) & (df['연면적(㎡)'] < 30000)].copy()

# 3. 양천구 양식에 맞춰 컬럼 구성
new_df = pd.DataFrame()
new_df['연번'] = range(1, len(filtered) + 1)
new_df['건물명'] = filtered['건물명']
new_df['대지위치'] = filtered['대지위치']
new_df['연면적(제곱미터)'] = filtered['연면적(㎡)']
new_df['주용도'] = filtered['주용도']
new_df['건물관리업체 여부'] = ''
new_df['관리업체명'] = ''
new_df['관리업체 연락처'] = ''
new_df['허가일'] = filtered['허가일']
new_df['사용승인일'] = filtered['사용승인일']
new_df['최대지상층수'] = filtered['최대지상층수']
new_df['설계사무소명'] = filtered['설계사무소명']

# 4. 새로운 시트로 저장
with pd.ExcelWriter(guro_file, mode='a', engine='openpyxl', if_sheet_exists='replace') as writer:
    new_df.to_excel(writer, sheet_name='중대형건물_관리현황', index=False)

print("구로구 파일에 '중대형건물_관리현황' 시트 생성 완료")
```

## 4. 트레이드오프 (Trade-offs)
- 원본 파일의 용량이 크거나 저장 중 충돌이 날 수 있으므로, 실행 시 파일이 닫혀 있어야 함.
- 구로구 원본 데이터 중 `연면적(㎡)`이 문자열이나 잘못된 형식일 경우를 대비해 수치형 변환 로직이 추가될 수 있음(to_numeric 적용).
"""

with open(r'd:\Dev\Project\sample\doc\plan.md', 'a', encoding='utf-8') as f:
    f.write(plan_addition)

dial_content = """# 2026-05-21 대화 기록
- 생성 시간: 2026-05-21T08:25:48+09:00
- 요청: 양천구파일 '중대형건물_관리현황' 시트를 참고해서 구로구 파일에서 연면적 10000 이상 30000미만인 목록으로 새 시트를 생성해줘
- 답변: 구로구 엑셀 파일과 양천구 엑셀 파일 구조를 리서치 완료하고, 구현 계획을 Artifact 및 plan.md에 업데이트하여 검토 요청 드렸습니다.
"""

with open(r'd:\Dev\Project\sample\doc\dial_260521.md', 'w', encoding='utf-8') as f:
    f.write(dial_content)

